---
name: install-proxmox-hetzner
description: Install and verify Proxmox VE on a bare-metal server through its provider's rescue system using QEMU with mandatory legacy BIOS. Hetzner dedicated servers are the tested platform. Use for an authorized fresh Proxmox installation or a preflight inspection. Does not deploy guests or reverse proxies.
---

# Install Proxmox through a provider rescue system

This entry exists so Claude Code discovers the skill. The skill itself is the tool-agnostic file at `skills/install-proxmox-hetzner/SKILL.md` in the repository root. Read that file in full and follow it. Do not act on this stub alone.

Before following it, read the root `AGENTS.md`, then `infrastructure/proxmox-hetzner/AGENTS.md` and `infrastructure/proxmox-hetzner/README.md`.
